# Docker Compose variables only.
# goduckbot reads all config from config.yaml.

# Image version
GODUCKBOT_VERSION=latest

# PostgreSQL password (only needed with --profile postgres)
# Must match database.password in config.yaml
GODUCKBOT_DB_PASSWORD=
